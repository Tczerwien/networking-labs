# Skeleton — fill in the body yourself, don't AI-generate.
import socket

HOST = '127.0.0.1'
PORT = 9999

# Create a UDP socket
# Bind to (HOST, PORT)
# Loop forever:
#   Receive data + addr
#   Print what was received
#   Send data back to addr
